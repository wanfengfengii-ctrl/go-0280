# 全株玉米青贮窖开窖前分层取芯检验与安全裁定闭环

## 项目目标

建设一个面向奶牛牧场的单体 Go Web 应用，以一次全株玉米青贮窖开窖前检验任务为唯一主流程。系统在锁定阶段固化地块、收割批次、切碎长度规则、接种剂摘要、窖体几何、分区与压实层、覆膜封签、预计开窖面、三维采样格、盲码、实验孔位、通风设备、安全阈值、发酵判定规则和任务代次；随后依次执行覆膜核验、受控取芯、孔口封堵、样本分装、理化检测、确定性扩检、开窖面通风核验及双人复核。后端采用嵌入式关系数据库实现事务持久化、启动恢复、幂等日志、租约与终局凭据保存；前端提供一张真实可操作的任务工作台页面，通过 HTTP API 展示三维采样进度、资源租约、证据、扩检范围和终局状态。生产实现控制在20至30个 Go 文件、约2500行有效生产 Go 代码、4至6个业务包内；前端另含 package.json、锁文件和固定版本的确定性构建脚本，不计入 Go 行数。

## 端到端业务流程

1. 目录建档与任务锁定：管理员维护青贮窖几何、种植地块、收割批次、切碎长度规则、接种剂、压实层、覆膜封签、预计开窖面、仪器和阈值；创建任务后校验地块与收割批关系、规则摘要版本、采样格完整性及坐标唯一性，并在单事务中生成不可变锁定快照和任务代次。
2. 覆膜核验与原子开采：两次提交同一覆膜封签按幂等规则处理，不同内容则拒绝；核验完整后，系统按确定顺序获取钻机、实验孔位和相斥窖区的限时租约，并与首个采样点建立原子提交，任务进入取芯中。
3. 顺序取芯与封堵：采样格按窖体分区、压实层序号、锁定深度由外向内推进，暴露前沿不可回退或跨越；每个孔必须完成原芯登记、分装封签和孔口封堵，才允许进入下一孔。租约超期可由恢复流程按逻辑时钟判定，但存在待重试仪器调用或未封堵孔时不得提前释放。
4. 样本封存与检测：原芯只能一次性拆分为检测样、留样和合理损耗，三者质量须守恒；封签形成不可拆分、只追加的交接链。盲码仅能在规定检测阶段由授权动作揭示。pH计、近红外仪、色谱仪和气体探头提交带逻辑序号的读数，乱序或迟到结果被保存为审计事实但不能覆盖较新有效证据。
5. 异常扩检：任一取芯点触发丁酸、氨态氮、霉菌毒素或温升异常后，按锁定邻接表扩展相邻压实层，并传播至同收割批影响坐标；系统排序、去重后一次性固化扩检集合并提升代次。并发扩检请求只允许一个代次获胜，旧代次结果不得闭合或覆盖新代次证据。
6. 通风与双人终局：采样、守恒、发酵检测及扩检证据闭合后，预计开窖面按外向内前沿进行连续通风；固定窗口内氧气与硫化氢有效读数均满足阈值才进入待复核。两名不同且合格的人员独立复核后，可开窖、饲料隔离或取消命令竞争同一终局单写屏障，唯一胜者生成凭据；已开窖、饲料隔离、已取消均不可再变更。

## 核心组件与职责

1. 青贮窖与发酵规则目录：管理窖体几何、分区、压实层、地块和收割批关联、切碎长度、接种剂、覆膜封签、开窖面、仪器能力以及整数定点阈值；提供带摘要版本的只读快照，禁止已锁定任务引用的历史版本被原位修改。
2. 开窖检验任务聚合：实现待锁定、覆膜核验中、取芯中、样本封存中、发酵检测中、扩检中、通风核验中、待复核、可开窖、已开窖、饲料隔离和已取消状态机，维护任务代次、暴露前沿、幂等操作记录、审计事件及终局单写屏障。
3. 分层采样及资源租约管理器：验证“窖体分区—压实层—取芯深度”三维采样格、缺层和重叠，强制深度顺序与孔间封堵屏障；以逻辑时钟管理钻机、孔位和相斥窖区租约，并保证资源获取、首点建立、续租和释放的事务一致性。
4. 样本封签与理化证据记录器：维护盲码、原芯、分装质量、损耗、封签交接链、仪器调用、读数版本和待重试队列；使用整数定点算法判定乳酸、丁酸、氨态氮、pH、干物质、霉菌毒素及温升，检查符号、舍入、除零和溢出。
5. 扩检通风及终局仲裁器：依据锁定邻接规则和同收割批关系计算确定扩检集合，隔离不同任务代次证据；验证连续通风窗口、氧气和硫化氢阈值、两名不同合格复核人，并仲裁三个竞争终局及生成唯一凭据。
6. Go HTTP API：提供目录、任务锁定、覆膜核验、租约、取芯、封堵、分装、检测、重试、扩检、通风、复核与终局端点；托管构建后的前端工作台，统一输出稳定错误码、确定排序原因、操作号冲突信息和任务快照。

## 领域规则与不变量

1. 任务锁定快照必须同时包含窖体几何摘要、地块、收割批次、切碎长度规则摘要、接种剂摘要、分区、压实层、覆膜封签、预计开窖面、采样格、取芯深度顺序、盲码、实验孔位、通风设备、氧气与硫化氢阈值、发酵规则和初始代次；任一目录摘要陈旧或地块与收割批错配均整体拒绝。
2. 三维采样坐标由窖区、压实层和深度组成，必须落在锁定几何内、无重复、无重叠并覆盖规则要求的全部层和深度；比较与输出统一按窖区、压实层、深度、盲码、孔位升序。
3. 开窖暴露前沿只能从外向内单调推进。每孔严格执行建立采样点、登记原芯、分装封签、完成必要检测或形成待重试、封堵孔口的顺序；未封堵不得推进下一孔、释放互斥资源或扩大前沿。
4. 钻机、实验孔位和配置为互斥的窖区采用限时排他租约。资源获取与首采样点创建必须同事务提交；冲突或失败时不得留下部分租约或采样覆盖。租约期限、续租次数和故障重试均由注入式逻辑时钟决定。
5. 盲码在授权检测步骤前不可揭示；样本封签链只允许追加完整交接记录，不允许拆链、改写或跳过持有人。原芯质量必须等于检测样、留样与允许损耗之和，所有质量均为非负整数定点值。
6. 理化量采用规则声明的整数缩放和确定舍入方式，计算前验证量纲与符号，乘除使用溢出检查，分母为零直接拒绝。读数按任务代次、调用逻辑序号和仪器序号判新旧；迟到或乱序值不可替换当前证据。
7. 仪器拒绝、断连、超时或格式错误只能生成包含逻辑时间、故障分类和确定重试次数的待重试调用，不能伪造读数、闭合检测、推进覆盖或提前释放资源；超过锁定重试上限后转入待复核并记录阻断原因。
8. 异常扩检集合等于异常坐标的锁定相邻层坐标与同收割批影响坐标的有序并集。计算与代次提升必须一次提交；同一源证据重复请求幂等，不同范围内容冲突，旧代次证据只能审计、不能影响新代次闭合。
9. 通风窗口必须由同一开窖面的连续有效气体读数组成，读数间隔不得超过锁定上限，窗口内氧气均不低于下限且硫化氢均不高于上限；格式错误、探头故障、窗口中断或旧代次读数均使窗口无效。
10. 相同操作号、相同规范化内容返回原结果；相同操作号、不同内容返回稳定幂等冲突码。所有命令以数据库事务执行，拒绝不得遗留部分租约、覆盖、封签流转、扩检范围、证据闭合或终局。
11. 只有全部采样格闭合、分装守恒、发酵和扩检证据合格且通风窗口有效时，才允许两名不同合格人员独立复核。终局比较并交换只允许可开窖、饲料隔离、已取消之一成功，成功生成任务内唯一凭据，终态后所有业务写命令均拒绝。

## 数据模型与持久化

1. CatalogSnapshot：目录快照ID、版本、几何摘要、地块ID、收割批ID、切碎长度规则、接种剂摘要、分区与压实层定义、邻接表、开窖面、设备清单、阈值和发酵规则摘要。
2. InspectionTask：任务ID、窖体ID、锁定快照ID、状态、当前代次、暴露前沿、创建与更新逻辑时间、阻断原因、版本号和终局凭据ID。
3. SamplingCell与CoreHole：窖区、压实层、深度、批次、盲码、孔位、顺序号、覆盖状态、原芯质量、封堵状态、建立代次及关联租约。
4. ResourceLease：资源类型、资源ID、任务ID、孔ID、租约令牌、获取时间、到期时间、续租次数和释放原因；数据库唯一约束保证同一有效资源只属于一个任务。
5. SampleSeal与SealTransfer：封签ID、盲码、样本类型、整数质量、损耗原因、当前持有人，以及只追加的来源、去向、逻辑时间、操作号和链序号。
6. InstrumentCall与EvidenceReading：调用ID、仪器类型、孔位、指标、任务代次、调用序号、状态、故障分类、重试次数、整数值、缩放因子、读数逻辑时间、有效性和拒绝原因。
7. ExpansionPlan：任务ID、源异常证据、代次、已排序的扩检坐标集合、同收割批影响集合、计算摘要、闭合状态和版本；任务与代次构成唯一键。
8. VentilationWindow与Review：开窖面、设备、代次、起止逻辑时间、有序气体读数、连续性结论，以及复核人、资格快照、独立意见和提交时间。
9. IdempotencyRecord、AuditEvent与FinalCredential：操作号、请求摘要、响应摘要、事件序列号、状态变更、错误码；终局类型、唯一凭据编号、胜出命令和生成逻辑时间。
10. 持久化使用嵌入式关系数据库及显式迁移；启动时恢复未完成任务、有效租约、待重试调用、扩检代次和终局屏障。恢复只依据持久化逻辑时间与记录，不依赖墙钟竞态。

## 公开接口

1. HTTP JSON API：/api/catalogs、/api/tasks、/api/tasks/{id}/lock、/film-checks、/leases、/cores、/seals、/readings、/retries、/expansions、/ventilation、/reviews 和 /finalize；所有写端点要求 operation_id、expected_generation 与规范化请求体摘要。
2. 错误协议：返回 stable_code、message、task_generation 和 reasons；reasons字段包含窖区、压实层、深度、盲码、孔位及具体约束，并按统一排序器输出。稳定码至少覆盖 BATCH_MISMATCH、STALE_RULE_DIGEST、DUPLICATE_FILM_SEAL、GRID_GAP、GRID_OVERLAP、LEASE_CONFLICT、HOLE_NOT_PLUGGED、BLIND_CODE_EARLY、MASS_NOT_CONSERVED、READING_STALE、FIXED_POINT_OVERFLOW、INSTRUMENT_RETRY_PENDING、GENERATION_CONFLICT、IDEMPOTENCY_CONFLICT 和 FINAL_ALREADY_WRITTEN。
3. 前端工作台：单页展示任务状态轨迹、三维采样格表、由外向内的暴露前沿、当前租约倒计时、封签链、仪器待重试、扩检集合、通风窗口和双人复核；允许执行当前合法动作并将后端的确定原因定位到对应采样格。
4. 前端工程包含 package.json、固定版本依赖的锁文件、源码与构建配置；使用锁文件冻结安装、固定构建命令生成静态资源，由 Go embed 托管。公开测试可直接使用预构建静态资源，不依赖联网安装。
5. 内部端口接口包括 Repository事务边界、LogicalClock、InstrumentAdapter、LeaseCoordinator和EventSink；公开测试通过内存或临时数据库、脚本化仪器和可推进逻辑时钟注入确定行为。
6. 工程建议划分 cmd/server、internal/catalog、internal/task、internal/sampling、internal/evidence、internal/arbitration、internal/httpapi 和 internal/store；核心业务包不少于4个，生产 Go 文件不少于20个，总有效生产 Go 代码保持在2000至3000行附近、目标约2500行。

## 失败边界

1. 锁定边界：目录版本、地块批次关系、封签唯一性或采样格任一校验失败时，任务保持待锁定，不写入半成品快照或部分网格。
2. 采样事务边界：租约竞争、错误代次、越序、未封堵或重复坐标导致整个命令回滚；资源租约与首点创建同成同败。
3. 封签与证据边界：质量不守恒、盲码提前揭示、封签链断裂、读数格式错误或定点异常均不得改变覆盖状态；原始拒绝事实可作为独立审计事件保存。
4. 外部仪器边界：拒绝、断连和超时转换为确定的待重试调用；恢复后根据调用序号继续，既不重复采纳读数，也不因进程重启跳过重试。
5. 扩检代次边界：扩检集合计算、代次提升和任务状态切换在一个事务内；并发请求只有一个新代次成功，旧代次迟到证据不可污染当前闭合判断。
6. 终局边界：复核、开窖、隔离与取消竞争由数据库唯一约束和版本检查保护；失败命令不生成凭据。进程在提交后响应前崩溃时，相同操作号重试返回已持久化的唯一结果。

## 验收标准

1. 锁定命令一次性固化窖体几何摘要、地块与收割批、接种剂、压实层、覆膜封签、三维采样格、盲码、检测孔位、通风设备、全部阈值和任务代次；错配、摘要陈旧、缺层、重叠或重复封签以稳定码整体拒绝。
2. 同一钻机、实验孔位或相斥窖区不能被并发任务同时占用，资源获取与首个采样点建立原子完成；提交失败或服务重启不遗留孤立租约或采样覆盖。
3. 取芯严格按锁定的外向内深度和孔序推进，每孔分装守恒且封堵后才可进入下一孔；相同操作号同内容重试返回相同结果，内容冲突、错误代次和越序操作均不改变状态。
4. 质量及全部理化指标使用整数定点计算，并可确定验证非负、舍入、除零和溢出边界；pH计、近红外仪、色谱仪或气体探头故障只产生按逻辑时间排序、重试次数确定的待重试调用。
5. 丁酸、氨态氮、霉菌毒素或温升异常会一次性生成按坐标排序去重的相邻层与同收割批扩检集合；并发扩检仅提升一个代次，新代次证据不被旧代次迟到结果覆盖。
6. 只有采样、分装、发酵和扩检全部闭合，且开窖面连续通风窗口内氧气与硫化氢均合格，任务才进入待复核；两名不同且资格有效的人员完成独立复核后才可形成可开窖结论。
7. 开窖、饲料隔离和取消并发竞争只产生一个不可变终局与一份唯一凭据；终态后的采样、检测、扩检、通风及复核请求均以稳定错误码拒绝且不产生业务变更。
8. 项目可从空目录确定构建并运行：Go 后端含真实数据库迁移和重启恢复，前端含 package.json与锁文件且可确定构建；生产 Go 代码不少于20个文件、约2000至3000有效行，公开测试不少于4个文件和12个独立用例并全部通过。

## 确定性测试场景

1. 锁定成功用例精确断言所有目录摘要、阈值、设备、盲码和初始代次被固化，之后修改目录不影响已锁定任务。
2. 使用虚构窖体坐标分别构造越界、缺压实层、重复坐标和深度区间重叠，断言稳定错误码、确定原因排序且数据库无部分网格。
3. 两个任务通过同步屏障并发抢占同一钻机、孔位和相斥窖区，断言仅一方取得完整租约并建立首点，失败方无任何租约或覆盖。
4. 以固定层和深度验证外向内顺序；尝试跨深度、未分装推进和未封堵开下一孔，断言状态、前沿和租约均不变。
5. 以整数质量验证检测样加留样加损耗精确守恒，并覆盖负数、差一最小单位、允许损耗边界和相同操作号冲突。
6. 对pH、乳酸、丁酸、氨态氮比例、干物质和霉菌毒素执行阈值等值、向上或向下舍入、除零以及乘法溢出测试，断言无浮点差异。
7. 脚本化四类仪器依次返回拒绝、断连、超时、格式错误和成功；推进逻辑时钟，精确断言待重试次数、调用次序、租约不提前释放及最终只采纳一次读数。
8. 先提交较新读数，再提交乱序或迟到读数，断言后者仅进入审计记录；提前揭示盲码时断言封签链和检测状态均未改变。
9. 构造单点丁酸异常，精确断言相邻压实层与同收割批坐标的排序并集；通过同步屏障触发扩检代次竞争，断言只生成一个新代次。
10. 在新代次建立后送达旧代次检测结果，断言扩检闭合状态不变；新代次全部证据到齐后才能进入通风核验中。
11. 使用逻辑时间提交合格、不合格、间隔过大和故障气体读数，验证连续窗口、氧气下限、硫化氢上限及两名不同合格复核人的屏障。
12. 三个并发协程同时请求开窖、饲料隔离和取消，断言唯一终态、唯一凭据和其余稳定冲突码；重启数据库后再次请求仍返回同一终局。
13. 在租约、待重试调用、扩检计划和终局分别落盘后模拟进程重启，断言恢复结果完全由逻辑时钟决定且无重复事件或孤立状态。
14. HTTP集成测试加载构建后的前端页面并调用完整闭环 API，断言页面资源可用、JSON错误结构稳定、原因排序一致及幂等响应字节级可复现。

## 组件追踪关系

1. 青贮窖与发酵规则目录对应锁定完整性、地块与收割批错配、规则摘要陈旧、几何与阈值冻结，以及验收1。
2. 开窖检验任务聚合对应完整状态机、操作幂等、代次控制、重启恢复和终局写屏障，以及验收3、7、8。
3. 分层采样及资源租约管理器对应三维网格、外向内前沿、钻机与孔位竞争、孔口封堵和原子首点，以及验收2、3。
4. 样本封签与理化证据记录器对应盲码、不可拆分交接链、质量守恒、整数定点、仪器故障与迟到读数，以及验收3、4、5。
5. 扩检通风及终局仲裁器对应异常传播、代次隔离、连续通风窗口、双人复核与并发终局，以及验收6、7、8。
6. Go HTTP API与前端工作台对应稳定错误协议、确定原因排序、真实页面交互、确定构建和端到端公开测试；持久化适配层作为各组件共享基础设施，不另立核心业务组件。

## 独特性

本计划聚焦奶牛牧场全株玉米青贮窖开窖前的三维分层取芯：以不可回退的暴露前沿串联钻机与孔位租约、封签交接链、发酵异常跨层及同收割批传播、气体安全窗口和双人终局屏障，区别于原料入厂、发酵转序或一般食品放行项目。
